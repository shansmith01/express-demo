describe('User service', () => {
  // let userService: UserService
  // const mockUser = userFactory.build()

  beforeEach(() => {
    // userService = new UserService()
  })

  afterEach(() => {
    // TODO
  })

  describe('create', () => {
    describe('valid', () => {
      it('should make a request to Sendbird and return the user for a valid request', async () => {
        expect('todo').toEqual('todo')
      })
    })
  })
})
